import { Code2, ExternalLink } from "lucide-react";
import { MarkdownContent } from "@/components/markdown/markdown-content";
import type { ProjectTechnologyRecord } from "@/features/projects/queries";
import type { ProjectLifecycleStatus } from "@/features/projects/validation";
/* eslint-disable @next/next/no-img-element -- Covers may use runtime managed-media URLs or validated external compatibility URLs. */
function formatProjectDate(value: string | null) { if (!value) return null; return new Date(`${value}T00:00:00Z`).toLocaleDateString(undefined, { month: "short", year: "numeric", timeZone: "UTC" }); }
export function ProjectPresentation({ coverImageUrl, demoUrl, endedOn, externalUrl, githubUrl, html, projectStatus, startedOn, summary, technologies, title }: { coverImageUrl: string | null; demoUrl: string | null; endedOn: string | null; externalUrl: string | null; githubUrl: string | null; html: string; projectStatus: ProjectLifecycleStatus; startedOn: string | null; summary: string; technologies: ProjectTechnologyRecord[]; title: string }) { const start = formatProjectDate(startedOn); const end = formatProjectDate(endedOn); return <article><header className="mb-8 border-b border-slate-200 pb-7"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-[var(--public-accent)]">Project · {projectStatus}</p><h1 className="mt-3 font-serif text-4xl font-semibold tracking-tight text-slate-950 sm:text-5xl">{title}</h1>{summary ? <p className="mt-4 text-lg leading-8 text-slate-600">{summary}</p> : null}<div className="mt-4 flex flex-wrap items-center gap-3 text-sm text-slate-500">{start ? <span>{start}{end ? ` – ${end}` : projectStatus === "active" ? " – present" : ""}</span> : null}{technologies.length ? <ul aria-label="Technologies" className="flex flex-wrap gap-2">{technologies.map((technology) => <li className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-700" key={technology.id}>{technology.name}</li>)}</ul> : null}</div><div className="mt-5 flex flex-wrap gap-4">{githubUrl ? <a className="inline-flex items-center gap-1.5 font-semibold text-[var(--public-accent)] underline underline-offset-4" href={githubUrl} rel="noopener noreferrer" target="_blank"><Code2 className="size-4" /> Source<span className="sr-only"> (opens in a new tab)</span></a> : null}{demoUrl ? <a className="inline-flex items-center gap-1.5 font-semibold text-[var(--public-accent)] underline underline-offset-4" href={demoUrl} rel="noopener noreferrer" target="_blank">Demo <ExternalLink className="size-4" /><span className="sr-only"> (opens in a new tab)</span></a> : null}{externalUrl ? <a className="inline-flex items-center gap-1.5 font-semibold text-[var(--public-accent)] underline underline-offset-4" href={externalUrl} rel="noopener noreferrer" target="_blank">Project site <ExternalLink className="size-4" /><span className="sr-only"> (opens in a new tab)</span></a> : null}</div></header>{coverImageUrl ? <img alt={`Cover for ${title}`} className="mb-8 h-auto w-full rounded-lg border border-slate-200 object-cover" src={coverImageUrl} /> : null}<MarkdownContent html={html} /></article>; }
