import { FilePlus2 } from "lucide-react";
import type { Metadata } from "next";
import Link from "next/link";
import { listPublications } from "@/features/publications/queries";
export const metadata: Metadata = { title: "Publications | PortfolioCMS" };
export default async function AdminPublicationsPage() { const items = await listPublications(); return <div className="mx-auto max-w-6xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-[0.14em] text-teal-800">Academic portfolio</p><h1 className="mt-2 font-serif text-3xl font-semibold tracking-tight">Publications</h1><p className="mt-2 text-slate-600">Manage scholarly work, ordered authors, attachments, and public visibility.</p></div><Link className="inline-flex min-h-11 items-center gap-2 rounded-md bg-slate-950 px-4 py-2 text-sm font-semibold text-white hover:bg-slate-800" href="/admin/publications/new"><FilePlus2 className="size-4" /> New publication</Link></div>{items.length === 0 ? <div className="mt-8 rounded-xl border border-dashed border-slate-300 bg-white px-6 py-12 text-center"><h2 className="font-serif text-xl font-semibold">No publications yet</h2><p className="mt-2 text-sm text-slate-600">Create a private draft to document your scholarly work.</p></div> : <div className="mt-8 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Publication</th><th className="px-5 py-3">Type</th><th className="px-5 py-3">Status</th><th className="px-5 py-3"><span className="sr-only">Actions</span></th></tr></thead><tbody className="divide-y divide-slate-200">{items.map((item) => <tr key={item.id}><td className="px-5 py-4"><p className="font-semibold">{item.title}</p><p className="mt-1 text-xs text-slate-500">/publications/{item.slug}{item.authors.length ? ` · ${item.authors.map((author) => author.name).join(", ")}` : ""}</p></td><td className="px-5 py-4 capitalize text-slate-600">{item.publicationType}</td><td className="px-5 py-4 capitalize text-slate-600">{item.status}</td><td className="px-5 py-4 text-right"><Link className="font-semibold text-teal-800 underline underline-offset-4" href={`/admin/publications/${item.id}`}>Edit</Link></td></tr>)}</tbody></table></div>}</div>; }
